package org.queens.app.imagesearchengine;

import static org.junit.Assert.*;

import java.awt.image.BufferedImage;
import java.io.File;
import java.io.IOException;
import java.util.Arrays;

import javax.imageio.ImageIO;

import org.queens.app.imagesearchengine.LoadAnImage;

import org.junit.Test;

public class Tests {

	@Test
	public void testHistogram() {
/*		LoadAnImage tester = new LoadAnImage();
		
		BufferedImage testImage = null;
		try {
			testImage = ImageIO.read(new File("src/test/resources/histogram_test.jpg"));
		} catch (IOException e) {
			e.printStackTrace();
		}
		
		int[][] histogram = tester.generateImageHistogram(testImage);
		
		for (int[] arr : histogram) {
			System.out.println(Arrays.toString(arr));
		}*/
		
		// TODO Finish test so that its automatic
	}

}
